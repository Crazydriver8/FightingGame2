using UnityEngine;
using System.Collections;

using SimpleJSON;

public class DecisionTreeAI : MonoBehaviour {
    JSONNode jsonDecisionTree;


    void Start()
    {
        // Get decision tree from JSON on server/local
        //jsonDecisionTree = JSON.Parse();
    }
    
    void OnRoundBegins(int RoundNumber)
    {

    }
}
